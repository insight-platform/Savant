[build-system]
requires = [
    'setuptools>=61.0',
    'pybind11[global]',
    'scikit-build',
]
build-backend = 'setuptools.build_meta'

[tool.black]
skip-string-normalization = true

[tool.pylint.messages_control]
max-line-length = 88
